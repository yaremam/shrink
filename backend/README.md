# Shrink! — Backend

The API and business logic backing the Shrink! weight tracking app.

This is currently a skeleton. No language, framework, or data store has
been chosen yet — that decision will get its own design decision record
under [`docs/tdr/`](../docs/tdr/) before any code lands here.
