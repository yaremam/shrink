# Shrink! — iOS

The iOS app for the Shrink! weight tracking app.

This is currently a skeleton. No language or project setup has been
chosen yet — that decision will get its own design decision record
under [`docs/tdr/`](../docs/tdr/) before any code lands here.
